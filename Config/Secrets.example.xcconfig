// Copy this file to Secrets.xcconfig (which is gitignored) and fill in real values.
// The Anthropic API key NEVER lives in the app. It sits server-side behind a proxy
// (see Implementation Phase 13). The app only knows the proxy URL.

// URL of the Cloudflare Worker (or equivalent) that fronts Anthropic + USDA FDC.
// During early development with no proxy yet, leave this blank — the app falls back
// to the AIEstimationService mock and OFF-direct search.
PICKLE_PROXY_URL =
